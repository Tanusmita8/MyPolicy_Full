# ============================================
# Customer Service Configuration (from Config Server)
# ============================================

server:
  port: 8081

spring:
  application:
    name: customer-service

  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb+srv://praticks2003_db_user:cPZqSJF3LPPsHSEv@cluster0.enwyvnr.mongodb.net/Backend_databases?retryWrites=true&w=majority}
      database: Backend_databases

logging:
  level:
    com.mypolicy.customer: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://discovery-service:8761/eureka}
  instance:
    prefer-ip-address: true