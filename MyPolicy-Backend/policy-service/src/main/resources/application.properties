server.port=8085
spring.application.name=policy-service

# MongoDB — same DB as data-pipeline (unified_portfolio, customer_details live in Backend_databases)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://praticks2003_db_user:cPZqSJF3LPPsHSEv@cluster0.enwyvnr.mongodb.net/Backend_databases?retryWrites=true&w=majority}
spring.data.mongodb.database=Backend_databases

# Logging
logging.level.com.mypolicy.policy=INFO

# Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Eureka Discovery
eureka.client.service-url.defaultZone=${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka}
eureka.instance.prefer-ip-address=true
